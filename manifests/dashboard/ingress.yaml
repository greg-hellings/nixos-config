apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: dashboard-tailscale
spec:
  ingressClassName: tailscale
  defaultBackend:
    service:
      name: headlamp
      port:
        number: 3001
  tls:
    - hosts:
        - headlamp
