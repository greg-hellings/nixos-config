builds:
  include:
    - '*.x86_64-linux.*'
    - nixosConfigurations.*
    - defaultPackage.x86_64-linux
    - homeConfigurations.*.activationPackage
    - darwinConfigurations.*
  exclude:
    - nixosConfigurations.linode  # Requires decrypted files
    - nixosConfigurations.2maccabees  # Raspberry Pi 4
