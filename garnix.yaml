builds:
  include:
    - '*.x86_64-linux.*'
    - nixosConfiguration.*
    - defaultPackage.x86_64-linux
    - homeConfigurations.*
    - darwinConfigurations.*
  exclude:
    - nixosConfiguration.linode  # Requires decrypted files
    - nixosConfiguration.2maccabees  # Raspberry Pi 4
